const dragg = document.querySelectorAll(".task");
const dropp = document.querySelectorAll(".DandD");
dragg.forEach((task) => {
  const removeBtn = task.querySelector(".close-sign");

  const taskTitle = task.querySelector(".task-title");
});
